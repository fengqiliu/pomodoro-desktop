# 概要设计说明（HLD）

| 项目名称 | Pomodoro 桌面番茄钟 |
| --- | --- |
| 文档版本 | 2.0.0（与代码版本一致） |
| 上游文档 | docs/需求规格说明书.md |
| 关联文档 | docs/DDD重构方案.md（分层依据与迁移记录）、docs/NSIS-中文化.md |
| 文档依据 | 项目源码（`src/`、`src-tauri/`），路径与模块名以当前代码为准 |

---

## 1. 引言

### 1.1 编写目的

描述系统的总体架构、模块划分、关键机制、数据结构、接口与错误处理策略，为开发维护与后续演进提供设计基线。

### 1.2 设计总原则

1. **分层（DDD）**：前端按 domain（纯规则）/ application（端口与用例）/ infrastructure（适配器）/ presentation（组装与展示）分层；计时机制收敛在引擎 hook；阶段结束的业务规则通过 `onPhaseComplete` 回调注入；领域与应用层不触碰 IO/React/Tauri，可直接单测。
2. **平台抽象**：原生能力统一走 `infrastructure/platform/` 适配器（惰性动态导入 Tauri API + `isDesktop()` 特性检测），浏览器端自动降级 no-op，避免运行时解析失败。
3. **纯函数领域层**：计时规则、统计、备份校验、原生计时器均为纯逻辑，不触碰 IO，可直接单测。
4. **持久化版本化**：localStorage 键带版本后缀，结构变更时递增后缀、不做迁移。

## 2. 总体架构

### 2.1 分层视图

```
┌─────────────────────────────────────────────────────────┐
│ presentation/  App.tsx + components/（TopBar·TimerView·  │
│   TasksView·StatsView·SettingsModal）+ hooks/ + i18n.ts │
│   （hooks 组装：状态、副作用、onPhaseComplete 业务收尾）│
├──────────────────────────────┬──────────────────────────┤
│ 计时引擎 hooks/              │ domain/（纯函数）         │
│  usePomodoroEngine.ts        │  timer/  tasks/          │
│  （倒计时机制、原生桥接、     │  stats/  settings/       │
│   代际取消）                  │  + shared/date.ts        │
├──────────────────────────────┼──────────────────────────┤
│ application/  ports.ts（端口）│ infrastructure/          │
│              backupService   │  platform/（Tauri 惰性    │
│                              │  加载与降级适配器）        │
│                              │  storage/  audio/        │
├──────────────────────────────┴──────────────────────────┤
│ 持久化      localStorage（版本化键）                     │
├─────────────────────────────────────────────────────────┤
│ 原生层  src-tauri/  lib.rs（组合根：装配/插件/命令注册）   │
│         timer/（engine.rs 墙上时间引擎 + commands.rs）    │
│         tray/（托盘菜单与窗口显隐）                       │
│         tauri-plugin-global-shortcut / notification     │
└─────────────────────────────────────────────────────────┘
```

### 2.2 技术栈

| 层 | 技术 |
| --- | --- |
| 桌面框架 | Tauri 2（tray-icon 特性）+ 插件（全局快捷键、通知） |
| 前端 | React 18、TypeScript 5.6（strict）、Vite 6 |
| 后端 | Rust edition 2021，`serde/serde_json` |
| 音频 | WebAudio API（无资源文件） |
| 国际化 | 手写双列字典 `{ key: [zh, en] }` + `{param}` 插值 |
| 测试 | Vitest（前端纯函数）、cargo test（Rust 单测） |

## 3. 模块设计

### 3.1 `presentation/App.tsx` —— 组装层

- **职责**：组合 `hooks/*` 状态/副作用钩子与 `components/*` 展示组件；自身仅保留 UI 态（tab/chartMetric）与派生值（progress/accent/week）。业务规则与副作用不在本文件。
- **hooks 分工**（均由 App 组装）：
  - `usePersistedState`：全部持久化领域状态（settings/tasks/dailyProgress/cycleFocusCount/noise/history）+ 启动时历史裁剪 + localStorage 持久化 + 午夜/前台跨日守卫。
  - `usePomodoroSession`：接线计时引擎与阶段完成业务规则（提示音 → 周期推进 → 今日统计 → 历史 → 任务计数 → 通知 → 决定是否自动衔接），并持有 ref 镜像（settings/cycleFocusCount/activeTaskId/tr/enginePhase）；注入引擎的回调：
    - `getPhaseSeconds(phase)`：阶段 → 配置秒数。
    - `getCompletionNotification()`：按「即将完成的阶段 + 下一阶段」生成原生通知文案；通知开关关闭或浏览器端返回 `undefined`。
    - `onPhaseComplete(finished, nativeNotificationSent)`：返回 `{ nextPhase, nextSeconds, autoStart }` 交给引擎执行。
  - `useTaskList`（任务操作 + 当前专注任务）、`useSettingsDialog`（弹窗开关、热键草稿确认制、恢复默认）、`useHotkey`（全局热键注册与错误态）、`useAppShortcuts`（窗口内 Space/R/S）、`useBackup`（剪贴板备份导入导出）、`useToast`/`useTheme`/`usePinned`/`useLanguage`/`useNoise`/`useDocumentTitle`（各自独立的 state + effect）。

### 3.2 `presentation/hooks/usePomodoroEngine.ts` —— 计时引擎

只拥有「倒计时机制」，不含业务规则。

- **对外接口**：`{ phase, secondsLeft, running, toggle, reset, switchTo, skip, syncIfIdle }`。
- **核心状态**：
  - `endAtRef`：墙上时间截止点，启动时计算 `now + seconds*1000`；1 秒绘制循环只做 `remaining = max(0, round((endAt-now)/1000))`。
  - `countdownActionRef`：每次开始/暂停/取消自增；异步（如原生 start）返回后校验 action 未变，否则补发取消并丢弃结果——防止旧请求覆盖新状态。
  - `nativeTimerGenerationRef / nativeTimerRunningRef / nativeTimerListenerReadyRef`：原生计时桥接状态。
  - `completionInFlightRef`：完成流程防重入；`toggleInFlightRef`：开始/暂停串行化（全局快捷键可能双触发）。

### 3.3 `domain/timer` —— 纯计时规则

- `completeFocusCycle(count, longEvery)`：完成一次专注后计算下一阶段与周期计数（达到间隔 → 长休且归零；否则 → 短休且 +1；入参防御：非法值回退 1、负值取 0）。
- `nextPhaseAfterCompletedBreak()`：休息完成 → 专注；`nextPhaseAfterSkip(phase)`：专注 → 短休，其余 → 专注（跳过不计入周期）。
- `resetDailyProgressIfNeeded(progress, today)`：日期不同则归零今日计数。
- `remainingSeconds(endAt, now)`、`focusesUntilLongBreak(count, longEvery)`。

### 3.4 `infrastructure/platform/` —— 平台抽象层

- `isDesktop()`（`runtime.ts`）：`window.__TAURI_INTERNALS__` 存在即桌面端。
- 适配器（实现 `application/ports.ts` 端口）：`windowAdapter.setAlwaysOnTop/isAlwaysOnTop`、`shortcutAdapter.register/unregister`、`nativeTimerAdapter.start/pause/cancel/listenCompleted`、`notificationAdapter.notifyPhaseDone(title, body)`（首次发送时才请求通知权限）。
- 模块级缓存已加载的 Tauri API 命名空间；所有调用 try/catch，失败返回 `null/false` 或静默；浏览器端原生计时器返回 `null` 走本地回退。

### 3.5 领域与应用支撑模块

| 模块 | 职责与要点 |
| --- | --- |
| `domain/settings` | `DEFAULT_SETTINGS`、`mergeState()`（不可信部分状态与默认值合并 + settings/noise 深合并 + 每日重置校验）、`defaultState()`；`todayKey()` 在 `shared/date.ts` |
| `domain/stats` | `History = Record<dateKey, DayRecord>`；`recordFocus`（追加当日记录）、`last7Days`（含今日的 7 天窗口、缺日补 0）、`pruneHistory(keepDays=60)` |
| `application/backupService` | `buildBackup(state, history)` 生成备份 JSON；`parseBackup(text)` 校验 app 标识与结构 → `mergeState` + `pruneHistory` → 返回可持久化结果，任何异常返回 `null`（坏数据不落盘） |
| `infrastructure/audio/noiseEngine.ts` | `NoiseEngine`：单例 AudioContext + 2 秒循环 Buffer（白：随机；棕：低通积分；粉：Voss-McCartney 近似）；`setVolume` 用 `setTargetAtTime` 平滑；`setType` 播放中重建 Buffer 无缝切换 |
| `infrastructure/audio/chime.ts` | 共享 AudioContext（避免实例上限/自动播放告警）；专注结束 `[880,660,523]` Hz、休息结束 `[523,659,784]` Hz，正弦振荡 + 指数衰减包络 |
| `presentation/i18n.ts` | `DICT: Record<key, [zh, en]>`；`translate(lang, key, params)` 支持 `{param}` 插值、缺键回退 key；`PHASE_KEY/NOISE_KEY/LANGS/weekdayLabel` 辅助 |

### 3.6 `src-tauri/` —— Rust 原生层（限界上下文 + 组合根）

**`lib.rs`（组合根）**

- 仅装配：注册 `NativeTimer` 状态、global-shortcut / notification 插件、三个命令（`timer::commands::*`）、托盘 setup、关闭到托盘拦截。
- 窗口生命周期：`CloseRequested` 拦截 → 隐藏到托盘（不退出），仅托盘「退出」终止进程。
- `timer/commands.rs`（IPC 适配器）：`native_timer_start` 启动计时并 spawn 完成 worker——先按传入通知配置发系统通知（`notificationSent` 记录成败），再 `emit("native-timer-completed", {snapshot, notificationSent})`。

**`timer/engine.rs`（计时引擎）**

- 数据：`Arc<Mutex<TimerState>>`（generation/running/endAtMs/remainingMs）+ 可注入 `Clock`（生产 `SystemClock`，测试用假时钟）。
- 操作：
  - `start(durationMs)`：代际 +1，`endAtMs = now + duration`，返回 `(TimerSnapshot, TimerTicket)`。
  - `pause()`：按墙上时间求剩余并保存；代际 +1 使进行中的 worker 轮询失效。
  - `cancel(expectedGeneration?)`：无参取消当前；带参仅当传入代际等于当前代际才取消——旧代际的定向取消不影响新计时器。
  - `poll(ticket)`：`Pending(remainingMs) / Completed(snapshot) / Cancelled`（代际不匹配即 Cancelled）。
- `spawn_completion_worker(ticket, on_complete)`：循环 poll，剩余 >1s 睡 1s，否则睡 `min(剩余, 1s)`，到期触发回调——托盘驻留时通知与事件依然可靠送达。

**`tray/`（托盘上下文）**

- `build_tray`：`TrayIconBuilder`（id `main`），复用 `tauri.conf.json` 打包图标；菜单「显示/隐藏 / 退出」；左键单击切换窗口显隐（`toggle_window`）。

## 4. 关键机制设计

### 4.1 墙上时间倒计时

不采用 interval 累加（易受后台节流/抖动影响），而是记录截止点 `endAt`，每次 tick 用 `now` 反算剩余。React 只负责绘制（1s paint loop）与业务规则，不承担计时精度责任。

### 4.2 双计时适配器

| 场景 | 开始 | 到期判定 | 通知 |
| --- | --- | --- | --- |
| 桌面端 | `native_timer_start` 交 Rust（快照回填 generation） | Rust worker 按墙上时间判定 → emit 事件（先通知后事件） | 原生 worker（`notificationSent` 透传前端去重） |
| 浏览器端 | 跳过原生桥接（`isDesktop()` 为假，`nativeTimerAdapter.start` 返回 null） | paint 循环发现 remaining ≤ 0 本地完成 | 无（`notifyPhaseDone` no-op） |

两端口径一致：同一份结束时间戳算法（`remainingSeconds`）。

### 4.3 代际取消与并发防护

- **代际**：前端 `countdownActionRef` 与 Rust `generation` 双重防线。任何异步结果回来先校验「代际/序号仍为最新」，否则丢弃或补发取消。
- **防重入**：`completionInFlightRef` 保证完成流程串行；`toggleInFlightRef` 保证开始/暂停不因全局快捷键双触发而错乱。
- **卸载清理**：引擎卸载时取消监听、取消原生计时；快捷键注册 effect 卸载时注销。

### 4.4 每日重置（5 个触发点）

今日统计日历绑定，周期计数跨日延续。校验点：① 启动加载；② 午夜定时器（`setHours(24,0,0,50)` 后自续）；③ 窗口 `focus`；④ `visibilitychange` 变为可见；⑤ 完成专注与持久化写入前防御性校验。

### 4.5 历史自动裁剪

`pruneHistory` 构造近 60 天合法键集合，仅保留命中的记录；触发点：启动、记录、导入。图表只取近 7 天。

### 4.6 备份安全导入

导出：完整 state + history → 剪贴板（失败 Toast）。导入：剪贴板 → 不可用时 `prompt` 粘贴兜底 → `parseBackup` 校验（app 标识、state/history 为对象）→ `mergeState` 默认值合并 + 历史裁剪 → 写入并 `location.reload()`；校验失败仅 Toast，不写盘。

### 4.7 设置保存与快捷键草稿

设置面板编辑期只更新 `hotkeyDraft`（草稿），点击「完成」校验非空后才提交；提交后由 effect 统一「注销旧键 → 注册新键」，失败置 `hotkeyError=registration` 并告警。`syncIfIdle` 保证暂停状态下修改时长后倒计时对齐新值，运行中的计时不受影响。

## 5. 数据设计

### 5.1 localStorage 键表

| 键 | 内容 | 结构变更策略 |
| --- | --- | --- |
| `pomodoro-state-v3` | settings/tasks/completedToday/focusMinutesToday/date/cycleFocusCount/noise | 递增后缀，不迁移 |
| `pomodoro-history-v1` | `Record<dateKey, DayRecord>` | 同上 |
| `pomodoro-theme` / `pomodoro-pinned` / `pomodoro-lang` | light\|dark / "1"\|"0" / zh\|en | — |

### 5.2 核心类型

- `Settings { focus, short, long, longEvery, sound, notifications, autoStartBreaks, autoStartFocus, hotkey, noiseType, noiseVolume }`
- `Task { id: uuid, title, done, pomodoros }`；`NoisePref { on, type, volume }`
- `PersistedState`（5.1 全量结构）；`DayRecord { date, dayIndex, pomodoros, minutes }`
- Rust：`TimerSnapshot { running, remainingMs, generation }`（camelCase 序列化，与 TS 契约对齐）；完成事件在其上扁平追加 `notificationSent`。

## 6. 接口设计

### 6.1 Tauri 命令（前端 → Rust）

| 命令 | 入参 | 返回 |
| --- | --- | --- |
| `native_timer_start` | `durationMs: u64`、`notification?: {title, body}` | `TimerSnapshot` |
| `native_timer_pause` | — | `TimerSnapshot`（暂停后剩余） |
| `native_timer_cancel` | `generation?: u64` | `TimerSnapshot` |

### 6.2 事件（Rust → 前端）

`native-timer-completed`：`TimerSnapshot & { notificationSent: boolean }`；前端按 `generation` 过滤过期事件。

### 6.3 原生权限（`capabilities/default.json`）

`core:default`、窗口置顶读写（allow-set/is-always-on-top）、global-shortcut register/unregister/is-registered、`notification:default`。

### 6.4 内部约定

- 组件一律 Props 驱动 + `tr(key, params?)` 文案函数；禁止直接访问 Tauri API。
- 持久化结构修改 → 递增键后缀。

## 7. 界面设计

- **组件树**：`App → TopBar + Tabs + (TimerView | TasksView | StatsView) + SettingsModal? + Toast?`。
- **窗口**：380×580、居中、不可缩放/最大化、可最小化；标题在运行中镜像倒计时。
- **主题机制**：CSS 变量 `:root` / `[data-theme="dark"]`；阶段强调色经 `App` 行内 `--accent` 覆盖（红/绿/蓝）。
- **设置面板交互**：Esc/遮罩点击关闭；快捷键编辑走草稿 + 错误提示；「恢复默认」保留噪音开关。

## 8. 错误处理与降级策略

| 场景 | 策略 |
| --- | --- |
| Tauri API 调用失败 / 浏览器环境 | 静默降级为 no-op（`null`/`false`/空函数），不阻塞功能 |
| localStorage 不可用或数据损坏 | best-effort；加载失败回退默认值 |
| 备份导入数据非法 | Toast「导入失败」，不写入任何存储 |
| 全局快捷键注册失败 | `hotkeyError=registration` 提示换组合键，应用功能不受限 |
| 通知权限被拒 | 跳过发送；桌面端 worker 的 `notificationSent` 让前端不重复尝试 |
| 音频不可用 / AudioContext 受限 | chime/noise 全程 try/catch，静默失败 |
| 持久化结构升级 | 递增键后缀；旧键自然废弃 |

## 9. 测试设计

- **前端（Vitest，4 文件 23 例）**：`domain/timer/timer.test.ts`（长休周期/跳过/跨日重置/剩余时间）、`domain/stats/focusHistory.test.ts`（记录/7 天视图/裁剪）、`domain/tasks/task.test.ts`（任务增删改）、`application/backupService.test.ts`（构建/校验/往返）。原则：只测纯领域/应用规则的外部行为，不测 React 渲染细节。
- **Rust（cargo test，5 例）**：暂停保留墙上时间、仅最新代际可完成、取消失效、旧代际定向取消不干扰新计时器、worker 在截止时间送达完成。
- **命令**：`npm test`；`cargo test --manifest-path src-tauri/Cargo.toml --lib native_timer`。

## 10. 构建与部署

```
npm install            # 依赖
npm run dev            # 浏览器开发 :1420（strictPort，Tauri 期望）
npm run build          # tsc -b && vite build → dist/
npm run tauri dev      # 桌面开发（Vite + 原生窗口）
npm run tauri build    # 先 beforeBuildCommand(npm run build)，再 Rust release + 打包
python3 gen_icons.py   # 重新生成图标 → src-tauri/icons/
```

- `tauri.conf.json`：`frontendDist: ../dist`、`devUrl: http://localhost:1420`；窗口 380×580；打包目标 all；Windows NSIS 含简体中文语言文件（`nsis/SimpChinese.nsh`）。
- 产物（macOS aarch64 实测）：`target/release/bundle/macos/Pomodoro.app` 与 `bundle/dmg/Pomodoro_2.0.0_aarch64.dmg`（约 2.9 MB）。


